<!DOCTYPE html>
<html lang="en">

<head>
    <title>LOGIN</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container">
        <h1>LOGIN</h1>
        <% if(error){ %>
            <!-- Kiểm tra xem biến "error" có giá trị hay không. -->
            <p class="text-danger">
                <!-- Hiển thị thông báo lỗi nếu "error" có giá trị. -->
                <%= error%>
            </p>
            <% } %>
                <form action="/user/login" method="post">
                    <!-- Biểu mẫu gửi dữ liệu đăng nhập đến "/user/login" bằng phương thức POST. -->
                    <div class="form-group">
                        <label for="email">email</label>
                        <!-- Nhãn cho trường email. -->
                        <input type="email" name="email" id="email" class="form-control"
                            value="<%= data ? data.email : '' %>">
                        <!-- Trường nhập email với giá trị mặc định từ biến "data". -->
                    </div>
                    <div class="form-group">
                        <label for="password">password</label>
                        <!-- Nhãn cho trường password. -->
                        <input type="password" name="password" id="password" class="form-control"
                            value="<%= data ? data.password : '' %>">
                        <!-- Trường nhập password với giá trị mặc định từ biến "data". -->
                    </div>
                    
                    <button type="submit" class="btn btn-info my-2">Login</button>
                    <!-- Nút "Login" để gửi biểu mẫu. -->
                </form>
    </div>
</body>

</html>