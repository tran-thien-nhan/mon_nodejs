<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>danh sách sách</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container">
        <div class="mt-2">
            <% if (message) { %>
                <div class="alert alert-success alert-dismissable">
                    <!-- <button type="button" class="close" data-dismiss="alert">&times;</button> -->
                    <strong>Success!</strong>
                    <%= message %>
                </div>
                <% } %>
                    <a href="/create" class="btn btn-success">thêm sách mới</a>
                    <a href="/book" class="btn btn-warning">back</a>
                    <form action="/search" method="get">
                        <input type="text" name="search" id="search" class="form-control mt-2"
                            placeholder="Search by book name">
                        <button type="submit" class="btn btn-primary mt-2">Search</button>
                    </form>
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th><a href="/sort" name="sort" class="btn btn-warning">price</a></th>
                                <th>Quantity</th>
                                <th>action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (books.length> 0) { %>
                                <% books.forEach(item=> { %>
                                    <tr>
                                        <td>
                                            <%= item.name %>
                                        </td>
                                        <td>
                                            <%= item.price %>
                                        </td>
                                        <td>
                                            <%= item.quantity %>
                                        </td>
                                        <td>
                                            <a href="/update/<%= item._id%>" class="btn btn-success">update</a>
                                            <a href="/delete/<%= item._id%>" class="btn btn-danger">delete</a>
                                        </td>
                                    </tr>
                                    <% }); %>
                                        <% } else { %>
                                            <p style="color:red; font-weight: bolder;">No results found.</p>
                                            <% } %>
                        </tbody>
                    </table>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>